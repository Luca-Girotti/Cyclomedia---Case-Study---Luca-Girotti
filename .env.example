# Copy to .env (kept out of git) and fill in.
ANTHROPIC_API_KEY=
APP_USERNAME=demo
APP_PASSWORD=

# Optional
# JUDGE_MODEL=claude-opus-5        # model used by the checker's AI judge
# SUMMARIZER_MODEL=claude-opus-5   # model used by the reference app
# ANTHROPIC_BASE_URL=              # only if you reach Claude through an API gateway
